<!DOCTYPE html>
<html>
	<head>
  		<title> <%= full_title(yield(:title)) %> </title>
  		<%= stylesheet_link_tag "jquery-ui-1.8.custom", "application", :media => "all" %>
  		<%= javascript_include_tag "jquery-1.4.2.min", "jquery-ui-1.8.custom.min", "rails", "application", "bootstrap" %>
  		<%= csrf_meta_tags %>
  		<%= render 'layouts/shim' %> 
	</head>
	<body>
  		<%= render 'layouts/header' %>
		<div class="container">
			<% flash.each do |name, msg| %>
			<%= content_tag :div, msg, :class => "alert alert-#{name == :notice ? "info" : "error"}"%>
			<% end %>

			<%= yield %>
			<%= render 'layouts/footer' %>
		</div>
	</body>
</html>
